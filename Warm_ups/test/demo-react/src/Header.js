import React from 'react';

//component, labeled with Uppercase, returns a part of the UI
function Header(){
  return (
    <header>
      <h1>Hello World</h1>
    </header>
  )
}

export default Header; //makes it available in other places